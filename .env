# Layered into every service's environment by ovr's dotenv() plugin. Edit live from the TUI:
# press `i` on a service → the `env` tab → `e` to change a value (applies on restart).
GUESTBOOK_TITLE=📖 ovr guestbook
POOL=3
